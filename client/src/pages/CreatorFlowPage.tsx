import { useState, type ReactNode } from "react";
import { Link, useLocation } from "react-router";
import { AlertTriangle, ArrowLeft, ArrowRight, Check, Copy, Eye, EyeOff, Gauge, Image, LayoutDashboard, Mic, MicOff, Radio, Settings, Signal, Sparkles, Square, Video, VideoOff, Volume2 } from "lucide-react";
import { Button } from "@/components/ui/Button";

const streamId = "room-aurora";
const basePath = `/creator/streams/${streamId}`;

const navItems = [
  { label: "Overview", path: "/creator", icon: LayoutDashboard },
  { label: "Room details", path: `${basePath}/new`, icon: Sparkles },
  { label: "Broadcast setup", path: `${basePath}/setup`, icon: Radio },
  { label: "Connection check", path: `${basePath}/check`, icon: Signal },
  { label: "Live control room", path: `${basePath}/live`, icon: Video },
];

function StudioFrame({ children, eyebrow, title }: { children: ReactNode; eyebrow: string; title: string }) {
  const pathname = useLocation().pathname;
  return <div className="min-h-[calc(100dvh-68px)] bg-background text-foreground"><div className="border-b border-border/70 bg-background/90 px-4 py-6 backdrop-blur-xl sm:px-6 lg:px-10"><div className="mx-auto max-w-[1440px]"><Link to="/" className="mb-5 inline-flex items-center gap-2 text-xs font-bold text-secondary transition hover:text-primary"><ArrowLeft className="h-3.5 w-3.5" /> Back to Pinklive</Link><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.22em] text-primary">Creator studio / {eyebrow}</p><h1 className="text-3xl font-black tracking-[-0.05em] sm:text-4xl">{title}</h1></div><div className="flex items-center gap-2"><span className="rounded-full border border-warning/30 bg-warning-light px-3 py-1.5 text-xs font-bold text-warning">Draft</span><Button variant="ghost" className="h-9"><Settings className="h-3.5 w-3.5" /> Preferences</Button></div></div></div></div><div className="mx-auto grid max-w-[1440px] gap-8 px-4 py-7 sm:px-6 lg:grid-cols-[220px_minmax(0,1fr)] lg:px-10 lg:py-10"><aside className="lg:sticky lg:top-24 lg:h-fit"><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.2em] text-secondary">Stream journey</p><nav className="flex gap-1 overflow-x-auto pb-2 lg:flex-col lg:overflow-visible">{navItems.map(({ label, path, icon: Icon }, index) => { const active = pathname === path || (path === "/creator" && pathname === "/creator"); return <Link key={label} to={path} className={`flex shrink-0 items-center gap-3 rounded-xl border px-3 py-2.5 text-xs font-bold transition lg:text-sm ${active ? "border-primary bg-primary text-primary-foreground shadow-lg shadow-primary/15" : "border-transparent text-secondary hover:border-border hover:bg-accent hover:text-foreground"}`}><span className={`flex h-6 w-6 items-center justify-center rounded-lg text-[10px] ${active ? "bg-primary-foreground/20" : "bg-accent"}`}>{active ? <Icon className="h-3.5 w-3.5" /> : index + 1}</span>{label}</Link>; })}</nav></aside><main className="min-w-0">{children}</main></div></div>;
}

function SectionCard({ children, className = "" }: { children: ReactNode; className?: string }) {
  return <section className={`rounded-2xl border border-border bg-accent/60 p-5 shadow-xl shadow-foreground/5 sm:p-6 ${className}`}>{children}</section>;
}

function StepFooter({ back, next, nextLabel = "Continue" }: { back?: string; next: string; nextLabel?: string }) {
  return <div className="mt-8 flex items-center justify-between border-t border-border/70 pt-5"><div>{back && <Link to={back} className="inline-flex items-center gap-2 text-sm font-bold text-secondary hover:text-foreground"><ArrowLeft className="h-4 w-4" /> Back</Link>}</div><Link to={next}><Button size="lg">{nextLabel}<ArrowRight className="h-4 w-4" /></Button></Link></div>;
}

function Dashboard() {
  return <StudioFrame eyebrow="overview" title="Your creator desk"><div className="grid gap-5 md:grid-cols-3"><SectionCard className="md:col-span-2"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-primary">Next broadcast</p><h2 className="mt-2 text-2xl font-black tracking-[-0.04em]">Make something worth staying for.</h2><p className="mt-2 max-w-md text-sm leading-6 text-secondary">Create a room, connect your broadcast software, and go live when everything feels ready.</p></div><div className="hidden h-12 w-12 items-center justify-center rounded-2xl bg-primary-light text-primary sm:flex"><Radio className="h-5 w-5" /></div></div><Link to={`${basePath}/new`} className="mt-7 inline-flex"><Button>Start a new stream <ArrowRight className="h-4 w-4" /></Button></Link></SectionCard><SectionCard><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-secondary">This month</p><p className="mt-4 text-4xl font-black tracking-[-0.06em]">12.4k</p><p className="mt-1 text-sm text-secondary">total viewer minutes</p><div className="mt-6 flex items-center gap-2 text-xs font-bold text-success"><span className="h-2 w-2 rounded-full bg-success" /> 18% up from last month</div></SectionCard></div><div className="mt-6 grid gap-5 md:grid-cols-3"><SectionCard><div className="flex items-center justify-between"><p className="font-extrabold">Recent streams</p><Link to={`${basePath}/summary`} className="text-xs font-bold text-primary">View all</Link></div><div className="mt-5 space-y-4"><MetricRow label="Late night stories" value="2.8k viewers" /><MetricRow label="Acoustic night" value="1.4k viewers" /><MetricRow label="Tech setup" value="980 viewers" /></div></SectionCard><SectionCard className="md:col-span-2"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-success-light text-success"><Check className="h-4 w-4" /></span><div><p className="font-extrabold">Your studio is ready</p><p className="mt-1 text-xs text-secondary">Stream key connected and account verified.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><StatusTile label="Encoder" value="Connected" /><StatusTile label="Account" value="Verified" /><StatusTile label="Last stream" value="2 days ago" /></div></SectionCard></div></StudioFrame>;
}

function MetricRow({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b border-border/60 pb-3 text-sm last:border-0 last:pb-0"><span className="font-semibold text-foreground">{label}</span><span className="text-xs text-secondary">{value}</span></div>; }
function StatusTile({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-background/60 p-3"><p className="text-[10px] font-bold uppercase tracking-wider text-secondary">{label}</p><p className="mt-2 text-sm font-extrabold">{value}</p></div>; }

function RoomDetails() {
  const [title, setTitle] = useState("Late night stories, no script");
  return <StudioFrame eyebrow="room details" title="Shape the room before people arrive."><SectionCard><div className="grid gap-7 lg:grid-cols-[1fr_280px]"><div className="space-y-6"><label className="block"><span className="flex items-center justify-between text-xs font-bold text-secondary"><span>Stream title</span><span>{title.length}/100</span></span><input value={title} onChange={(event) => setTitle(event.target.value)} maxLength={100} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4 text-sm font-semibold outline-none transition focus:border-primary focus:ring-4 focus:ring-primary/10" /></label><label className="block"><span className="text-xs font-bold text-secondary">Description</span><textarea defaultValue="A quiet room for honest conversations and whatever is on your mind." rows={5} className="mt-2 w-full resize-none rounded-xl border border-border bg-background p-4 text-sm leading-6 outline-none transition focus:border-primary focus:ring-4 focus:ring-primary/10" /></label><div className="grid gap-4 sm:grid-cols-2"><SelectField label="Category" value="Talk" /><SelectField label="Privacy" value="Public" /></div><div><p className="text-xs font-bold text-secondary">Tags</p><div className="mt-2 flex flex-wrap gap-2"><span className="rounded-full border border-primary/20 bg-primary-light px-3 py-1.5 text-xs font-bold text-primary">#Stories</span><span className="rounded-full border border-primary/20 bg-primary-light px-3 py-1.5 text-xs font-bold text-primary">#Chill</span><span className="rounded-full border border-border bg-background px-3 py-1.5 text-xs font-bold text-secondary">+ Add tag</span></div></div></div><div><div className="flex aspect-video items-center justify-center overflow-hidden rounded-2xl border border-border bg-background"><Image className="h-8 w-8 text-secondary" /></div><p className="mt-3 text-xs font-bold text-secondary">Room thumbnail</p><Button variant="ghost" className="mt-2 w-full">Upload image</Button><p className="mt-2 text-[11px] leading-5 text-secondary">16:9 recommended. JPG or PNG, up to 5MB.</p></div></div><StepFooter next={`${basePath}/setup`} nextLabel="Save and connect source" /></SectionCard></StudioFrame>;
}

function SelectField({ label, value }: { label: string; value: string }) { return <label className="block"><span className="text-xs font-bold text-secondary">{label}</span><select defaultValue={value} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 text-sm font-semibold text-foreground outline-none focus:border-primary focus:ring-4 focus:ring-primary/10"><option>{value}</option><option>Gaming</option><option>Music</option><option>Tech</option></select></label>; }

function BroadcastSetup() {
  const [revealed, setRevealed] = useState(false);
  const [copied, setCopied] = useState("");
  const copy = (value: string) => { navigator.clipboard?.writeText(value); setCopied(value); window.setTimeout(() => setCopied(""), 1600); };
  return <StudioFrame eyebrow="broadcast setup" title="Connect the source you already trust."><div className="grid gap-5 lg:grid-cols-[1fr_300px]"><SectionCard><div className="flex items-start justify-between gap-4 border-b border-border/70 pb-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-primary">RTMP connection</p><h2 className="mt-1 text-lg font-extrabold">Streaming software details</h2><p className="mt-1 text-sm text-secondary">Use these values in OBS, Prism, or Streamlabs.</p></div><Radio className="h-5 w-5 text-primary" /></div><CredentialRow label="Server URL" value="rtmp://live.pinklive.com/app/" copied={copied} onCopy={copy} /><div className="mt-5"><div className="mb-2 flex items-center justify-between"><label className="text-xs font-bold text-secondary">Stream key</label><button onClick={() => setRevealed(!revealed)} className="inline-flex items-center gap-1 text-xs font-bold text-primary hover:underline">{revealed ? <EyeOff className="h-3.5 w-3.5" /> : <Eye className="h-3.5 w-3.5" />}{revealed ? "Hide" : "Reveal"}</button></div><CredentialRow label="" value={revealed ? "live_sk_94827103984719283741" : "••••••••••••••••••••"} copied={copied} onCopy={copy} /></div><div className="mt-6 flex items-start gap-3 rounded-xl border border-warning/30 bg-warning-light p-4"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0 text-warning" /><p className="text-xs leading-5 text-warning">Never share your stream key. Anyone with access can broadcast to your room.</p></div><StepFooter back={`${basePath}/new`} next={`${basePath}/check`} nextLabel="Test connection" /></SectionCard><SectionCard><p className="font-extrabold">Need help connecting?</p><p className="mt-2 text-sm leading-6 text-secondary">Open your software settings and paste the server URL and stream key into the stream section.</p><div className="mt-5 space-y-3"><HelpRow label="OBS Studio" /><HelpRow label="Prism Live" /><HelpRow label="Streamlabs" /></div></SectionCard></div></StudioFrame>;
}

function CredentialRow({ label, value, copied, onCopy }: { label: string; value: string; copied: string; onCopy: (value: string) => void }) { return <div>{label && <label className="mb-2 block text-xs font-bold text-secondary">{label}</label>}<div className="flex items-center gap-2"><code className="min-w-0 flex-1 overflow-hidden text-ellipsis whitespace-nowrap rounded-xl border border-border bg-background px-4 py-3 text-xs text-foreground">{value}</code><Button variant="ghost" aria-label={`Copy ${label || "stream key"}`} onClick={() => onCopy(value)} className="h-11 w-11 shrink-0 p-0">{copied === value ? <Check className="h-4 w-4 text-success" /> : <Copy className="h-4 w-4" />}</Button></div></div>; }
function HelpRow({ label }: { label: string }) { return <div className="flex items-center justify-between border-b border-border/60 pb-3 text-sm last:border-0 last:pb-0"><span className="font-bold">{label}</span><ArrowRight className="h-4 w-4 text-secondary" /></div>; }

function ConnectionCheck() { const [ready, setReady] = useState(false); return <StudioFrame eyebrow="connection check" title="Make sure the room feels good before launch."><div className="grid gap-5 lg:grid-cols-[1.25fr_0.75fr]"><SectionCard><div className="flex aspect-video flex-col items-center justify-center rounded-2xl bg-foreground text-center"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-primary-foreground/10 text-primary-foreground/60"><VideoOff className="h-6 w-6" /></div><p className="mt-4 text-sm font-bold text-primary-foreground/80">Waiting for signal</p><p className="mt-1 max-w-xs text-xs leading-5 text-primary-foreground/40">Start your broadcasting software. Your camera feed will appear here.</p></div><div className="mt-5 flex items-center justify-between border-t border-border/70 pt-4"><span className="flex items-center gap-2 text-xs font-bold text-secondary"><Signal className="h-4 w-4" /> No source detected</span><Button variant="ghost" onClick={() => setReady(!ready)}>{ready ? <Check className="h-4 w-4 text-success" /> : <Signal className="h-4 w-4" />} {ready ? "Connection ready" : "Refresh"}</Button></div></SectionCard><SectionCard><p className="font-extrabold">System check</p><div className="mt-5 space-y-4"><CheckRow label="Broadcast source" status={ready ? "Connected" : "Waiting"} good={ready} /><CheckRow label="Microphone" status={ready ? "Detected" : "Not detected"} good={ready} /><CheckRow label="Camera" status={ready ? "Detected" : "Not detected"} good={ready} /><CheckRow label="Connection" status={ready ? "Excellent" : "Waiting"} good={ready} /></div><div className="mt-6 rounded-xl border border-info/30 bg-info-light p-4"><p className="text-xs leading-5 text-info">You can still change your room details after going live.</p></div><StepFooter back={`${basePath}/setup`} next={`${basePath}/live`} nextLabel="Open control room" /></SectionCard></div></StudioFrame>; }
function CheckRow({ label, status, good }: { label: string; status: string; good: boolean }) { return <div className="flex items-center justify-between border-b border-border/60 pb-3 last:border-0 last:pb-0"><span className="text-sm font-semibold">{label}</span><span className={`flex items-center gap-1.5 text-xs font-bold ${good ? "text-success" : "text-secondary"}`}><span className={`h-2 w-2 rounded-full ${good ? "bg-success" : "bg-secondary"}`} />{status}</span></div>; }

function LiveRoom() { const [muted, setMuted] = useState(false); const [camera, setCamera] = useState(true); const [ending, setEnding] = useState(false); return <StudioFrame eyebrow="live control room" title="You are live. Stay close to the room."><div className="mb-5 flex flex-wrap items-center gap-3"><span className="flex items-center gap-2 rounded-full border border-live/30 bg-live-light px-3 py-1.5 text-xs font-bold text-live"><span className="h-2 w-2 animate-pulse rounded-full bg-live" /> LIVE 00:18:42</span><span className="rounded-full border border-border bg-accent px-3 py-1.5 text-xs font-bold text-secondary">1,248 viewers</span><span className="rounded-full border border-success/30 bg-success-light px-3 py-1.5 text-xs font-bold text-success"><Gauge className="mr-1 inline h-3.5 w-3.5" /> Excellent connection</span></div><div className="grid gap-5 xl:grid-cols-[1.2fr_0.8fr]"><SectionCard><div className="flex aspect-video items-center justify-center rounded-2xl bg-foreground"><div className="text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-live text-primary-foreground"><Radio className="h-6 w-6" /></div><p className="mt-4 text-sm font-bold text-primary-foreground">Your live preview</p><p className="mt-1 text-xs text-primary-foreground/50">Late night stories, no script</p></div></div><div className="mt-5 grid grid-cols-3 gap-2"><Button variant="ghost" onClick={() => setMuted(!muted)} className="h-11">{muted ? <MicOff className="h-4 w-4 text-danger" /> : <Mic className="h-4 w-4 text-success" />}{muted ? "Muted" : "Mic on"}</Button><Button variant="ghost" onClick={() => setCamera(!camera)} className="h-11">{camera ? <Video className="h-4 w-4 text-success" /> : <VideoOff className="h-4 w-4 text-danger" />}{camera ? "Camera" : "Off"}</Button><Button variant="ghost" className="h-11"><Volume2 className="h-4 w-4 text-primary" /> Audio</Button></div></SectionCard><SectionCard className="flex min-h-[380px] flex-col"><div className="flex items-center justify-between border-b border-border/70 pb-4"><div><p className="font-extrabold">Live chat</p><p className="mt-1 text-xs text-secondary">42 messages this minute</p></div><Button variant="ghost" className="h-8 px-2 text-xs">Moderate</Button></div><div className="flex-1 space-y-4 py-5"><ChatLine name="Linh Nguyen" text="This room feels so cozy." /><ChatLine name="hoang.band" text="The audio is really clear tonight." /><ChatLine name="Mina" text="Can you take a song request?" /></div>{ending ? <div className="rounded-xl border border-danger/30 bg-danger-light p-4"><p className="text-sm font-extrabold text-danger">End this live stream?</p><p className="mt-1 text-xs text-danger/80">Your viewers will be disconnected immediately.</p><div className="mt-3 flex gap-2"><Button variant="danger" onClick={() => window.location.assign(`${basePath}/summary`)}>End stream</Button><Button variant="ghost" onClick={() => setEnding(false)}>Keep streaming</Button></div></div> : <Button variant="danger" onClick={() => setEnding(true)} className="w-full"><Square className="h-4 w-4" /> End stream</Button>}</SectionCard></div></StudioFrame>; }
function ChatLine({ name, text }: { name: string; text: string }) { return <div className="flex gap-3"><div className="h-7 w-7 shrink-0 rounded-lg bg-primary-light" /><div><p className="text-xs font-extrabold">{name}</p><p className="mt-0.5 text-xs text-secondary">{text}</p></div></div>; }

function Summary() { return <StudioFrame eyebrow="stream summary" title="That was a good room."><SectionCard><div className="flex flex-col justify-between gap-5 border-b border-border/70 pb-6 sm:flex-row sm:items-center"><div><span className="inline-flex items-center gap-2 rounded-full border border-success/30 bg-success-light px-3 py-1.5 text-xs font-bold text-success"><Check className="h-3.5 w-3.5" /> Stream ended successfully</span><h2 className="mt-4 text-2xl font-black tracking-[-0.04em]">Late night stories, no script</h2><p className="mt-1 text-sm text-secondary">Today · 18 minutes 42 seconds</p></div><Link to={`${basePath}/new`}><Button>Start another stream</Button></Link></div><div className="grid gap-3 py-6 sm:grid-cols-4"><SummaryMetric label="Total viewers" value="2,840" /><SummaryMetric label="Peak viewers" value="1,248" /><SummaryMetric label="Avg. watch time" value="08:42" /><SummaryMetric label="Chat messages" value="486" /></div><div className="rounded-2xl border border-border bg-background p-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-primary-light text-primary"><Sparkles className="h-4 w-4" /></div><div><p className="font-extrabold">A strong first signal</p><p className="mt-1 text-xs text-secondary">Your peak audience arrived during the middle of the conversation.</p></div></div></div></SectionCard></StudioFrame>; }
function SummaryMetric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-background p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-secondary">{label}</p><p className="mt-2 text-2xl font-black tracking-[-0.04em]">{value}</p></div>; }

export default function CreatorFlowPage() { const pathname = useLocation().pathname; if (pathname === "/creator") return <Dashboard />; if (pathname.endsWith("/new")) return <RoomDetails />; if (pathname.endsWith("/setup")) return <BroadcastSetup />; if (pathname.endsWith("/check")) return <ConnectionCheck />; if (pathname.endsWith("/live")) return <LiveRoom />; if (pathname.endsWith("/summary")) return <Summary />; return <Dashboard />; }
